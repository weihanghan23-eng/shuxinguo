document.addEventListener('DOMContentLoaded', function() {
    const pageContainer = document.querySelector('#page20-container');

    // 检查页面容器是否存在，以确保脚本不会在其他页面上意外运行
    if (!pageContainer) {
        return;
    }

    // --- 页面专属逻辑 ---
    // 原始文件 page20.html 中未发现 JavaScript 逻辑。
    // 如果需要添加交互，请在此处编写。
    // 所有 DOM 查询都应从 pageContainer 开始，例如：
    // const someElement = pageContainer.querySelector('.some-class');

});